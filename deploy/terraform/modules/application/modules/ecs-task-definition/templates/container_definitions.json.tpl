[
  {
    "name": "${name}",
    "essential": ${essential},
    "cpu": ${cpu},
    "memory": ${memory},
    "image": "${image}",
    "portMappings": ${portMappings},
    "environment": ${environment},
    "mountPoints": ${mountPoints}
  }
]